const expect = require('chai').expect;
const User = require('../src/models/user');

describe('user model', () => {
  it('Create user', () => {});
  it('Read all users', () => {});
  it('Read user by id', () => {});
  it('Update user', () => {});
  it('Delete user', () => {});
});
